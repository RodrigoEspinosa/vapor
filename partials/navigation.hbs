<nav class="menu" role="nav">
    <ul>
        {{#foreach navigation}}

        <li class="{{#if current}} current{{/if}}">
            <a href="{{url absolute=true}}">{{label}}</a>
        </li>

        {{/foreach}}
    </ul>
</nav>
